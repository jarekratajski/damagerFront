import {FunctionComponent} from "react";
import {AppProps} from "../../data/AppProps";
import {GameProps} from "../../data/Model";

export const Maze: FunctionComponent<GameProps> = game => {

    return <div>

    </div>;
}
